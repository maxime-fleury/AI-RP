[project]
name = "innsekai-image-server"
version = "0.1.0"
description = "Koji (SDXL) image generation sidecar for the Innsekai app"
requires-python = ">=3.11"
dependencies = [
    "diffusers>=0.31",
    "safetensors",
    "accelerate",
    "fastapi",
    "uvicorn",
    "pillow",
    "torch>=2.5",
]

[tool.uv]
package = false
