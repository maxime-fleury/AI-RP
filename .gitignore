# dependencies / build
node_modules
dist
*.log

# env / secrets
.env
.env.local

# local data (SQLite DB, audio, images, samples) — generated at runtime
data/

# models: Koji (4 GB+) — not uploaded
models/
python/image_server/.venv/
python/image_server/__pycache__/

# Python sidecars: virtualenvs and caches stay local
.venv/
**/__pycache__/

# agent workspace
.freebuff/
