{
  "name": "innsekai",
  "version": "0.1.0",
  "private": true,
  "description": "",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "check": "bun run typecheck && bun build src/index.ts --outdir /tmp/innsekai-build --target=bun && bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "5"
  },
  "dependencies": {}
}