{
  "name": "ai-rp",
  "version": "0.1.0",
  "private": true,
  "description": "",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "check": "bun build src/index.ts --outdir /tmp/airp-build --target=bun && bun test"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "dependencies": {}
}